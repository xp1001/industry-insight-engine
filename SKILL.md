---
name: industry-insight-engine
version: 2.0.2
homepage: https://github.com/xp1001/industry-insight-engine
description: 行业洞察引擎（industry-insight-engine）— 输入任意行业/品牌/话题 →
  快速了解、拆解、分析、判断、预测并给出行动指南。假设驱动 4 步 + 外围扫描，中国市场原生适配，支持 --refresh 增量更新。
  Deep 模式情报系统不可跳过。触发词：研究 XX / 分析 XX 行业 / 这个赛道怎么样 / XX 能赚吗 / 我要不要做 XX。
author: XP
created: 2026-06-28
agent_created: true
tags:
  - industry-research
  - competitor-analysis
  - opportunity-judgment
  - action-guide
  - decision-engine
---

# 行业洞察引擎 (Industry Insight Engine) v2.0.2

> **一句话**：输入行业/品牌/话题 → 输出一份包含**格局拆解、机会判断、预测和行动指南**的决策级报告，并可持续刷新。
>
> **v2.0 重构**：① 流程改为假设驱动（初步判断前置，检索以推翻假设为目标）；② 新增 `--refresh` 增量刷新；③ 21 项检查收敛为 9 道闸门，规范合并为 10 条硬规则；④ 移除伪精确图表（主观评估一律表格化呈现）。判断纪律资产（范围锁定/意图映射/判断三件套/信源留痕/官方模板）全部保留。

---

## 触发词与意图→档位映射

- "研究 XX" / "拆解 XX" / "分析 XX 行业"
- "这个赛道怎么样" / "XX 能赚吗" / "我要不要做 XX"
- "帮我看看 XX 行业" / "XX 值得入局吗"
- "XX 有什么新变化吗" / "更新一下 XX 的研究"

**不需要显式调用 skill**——上述自然语言直接触发。

**意图 → 档位映射**（执行前先判断用户在哪个决策阶段）：
- "扫一眼 / 快速看 / XX 是什么" → **Quick**（初筛）
- "怎么样 / 能赚吗 / 值得吗 / 有前景吗" → **Standard**（入口决策）
- "要不要做 XX / 我准备入场 / 该怎么切入 / 拆解这个品牌" 或显式 `--deep` → **Deep**（用户已过决策关，Standard 交付不了执行基建）
- "有什么新变化 / 更新一下" 或显式 `--refresh` → **Refresh**（已有产出的增量刷新，见四）
- 同一目标已跑过 Standard，用户追问"那我要做的话怎么办" → 视为升级 Deep

---

## 一、执行流程

```
输入 → S1 素描+范围锁定 → S1.5 初步判断（押注） → S2 格局拆解（定向找反证）
     → S2.5 外围扫描 → S3 机会判断（修正/维持押注） → S4 行动指南 → report.html
```

每步产出直接作为下一步的输入，不中断、不保存中间文件（Standard 和 Deep 最终输出 `analysis.md`）。

**深度分支**：Quick 模式只跑 `S1 → S3 结论（机会+风险，不含预测）`，跳过 S2 / S2.5 / S4；Quick 报告中的竞品信息仅来自 S1 的 Top 5 玩家简表（无赚钱模式/年营收列）。

**单品牌/本地客户 Deep 的流程适配**（四步为行业导向，输入单一品牌时按此调整）：
- S1 照跑行业素描——单品牌也需要行业坐标系；S1.5 押注对象为"该品牌/该模式是否值得加码"
- S2 的"竞品 5 核维"改为：**目标品牌必拆 + 2-4 个直接竞品/业态对照拆**
- 情报系统产出位置统一：一律写入 `deep-dive/intelligence.md`（行业 Deep 用行业信源，单品牌/本地 Deep 用本地竞争监控方案），并在 report.html 末尾内嵌摘要

---

## 二、深度三档

| 档位 | 触发 | 适用意图 | 做什么 | 报告等级 |
|------|------|---------|--------|---------|
| **Quick** | `--quick` / "快速看" | 初筛 | S1 → S3 结论 | **卡片式 HTML**：无图表，无 sidebar |
| **Standard** | 默认 | 入口决策 | 全流程 | **完整 HTML**：hero + 3 callout + 机会金矩阵 + sidebar |
| **Deep** | `--deep` / "深度" / 入场类意图 | 入场执行 | Standard + 单品牌深拆文件 + **情报系统（❌ 不可跳过）** | **完整 HTML** + `analysis.md` + `deep-dive/` |
| **Refresh** | `--refresh` / "有什么新变化" | 存量更新 | 增量刷新（见四） | 原报告更新 + 变更说明 |

---

## 三、硬规则表（全流程 10 条，替代散落规范）

| # | 规则 | 要点 |
|---|------|------|
| 1 | **范围锁定** | 过宽输入（如"研究新能源"）先收窄为"细分赛道+地域+渠道"一句话，并在报告开头注明收窄逻辑 |
| 2 | **押注前置** | S1 后必须给出初步判断（值得入场/谨慎观察/不建议）+ 该判断依赖的 2-3 条关键假设；S2/S2.5 检索以**推翻这些假设**为优先目标 |
| 3 | **信源分级与标注** | 优先级：财报/招股书 > 艾瑞/易观/沙利文/灼识/QuestMobile > 36氪/虎嗅/亿邦/联商/窄门餐眼 > 自媒体（仅线索）。每个关键数字带 年份+口径（GMV/营收/出货量/用户数）+来源；冲突给区间并说明口径差；核心数字优先近 24 个月 |
| 4 | **未公开规则** | 任何硬事实（营收/转化链路/差异化/工具栈）无公开证据一律写「未公开」，禁止推测补位；主观评估（如能力打分）只以表格呈现并标注「拆解评估」，**不得做成看似数据的图表** |
| 5 | **转引留级** | 未命中原文的转引数据标注"转引"，只用于趋势与量级判断；决策级数字须一手来源 |
| 6 | **留痕** | 所有外部信源在产出目录落盘 `sources.md`（URL+访问日期+支撑结论）；Refresh 时追加不覆盖 |
| 7 | **禁止硬凑** | 机会金矩阵空象限标注"空"+原因；冷门行业查不到玩家/规模时如实写"未形成可见格局"（检索不到本身是信号）；"未发现≠不存在"要写明 |
| 8 | **检索纪律** | 每步按预算检索；限流时等 30-60s 单发重试，最多 2 轮；查询被内容过滤拦截时换中性词重查（如"关店潮"→"关停并转"）并如实记录过滤发生；仍失败则记录并按未公开规则降级，不硬凑 |
| 9 | **结论先行与引导** | hero 必有一句话总体判断徽章（🟢/🟡/🔴）；下一步建议每条带可复制指令；报告末尾必有使用引导（想深入/想跟踪/有异议分别怎么问） |
| 10 | **Deep 铁律** | 情报系统不可跳过：≥3 个分级信源（≥1 数据平台+≥1 行业媒体+≥1 竞品官方渠道）+ 周/月/季节奏 + 预警规则 |

---

## 四、Refresh：增量刷新模式

**触发**：`--refresh` / "有什么新变化 / 更新一下"。**前提**：目标目录已有上一轮产出，否则转 Deep 首跑。

**流程**：
1. 读上一轮 `analysis.md` 的机会清单、风险预警与情报方案，提取 **3-5 个"若变化则动摇结论"的监控点**（如竞对动作、政策落地、关键假设）
2. 只针对监控点定向检索（**≤5 次**，遵守硬规则 8）
3. 三分支：
   - **无实质变化** → 更新 report.html 数据与日期，analysis.md 末尾追加一段刷新记录（日期+查了什么+结论不变），一页以内
   - **局部变化** → 只重写受影响章节，报告顶部加「更新说明」块（改了哪条结论、被什么证据触发）
   - **核心结论被推翻** → 明确告知用户，建议整体重跑（按重跑语义执行）

**产出纪律**：`sources.md` 追加新信源；refresh 记录必须可追溯（查了什么、没查什么）。

---

## 五、输出规格

### 文件结构

```
output/{slug}/
├── analysis.md          ← Standard + Deep：完整流程分析
├── report.html          ← 分享报告（官方模板）
├── sources.md           ← 信源留痕（硬规则 6）
└── deep-dive/           ← Deep only
    ├── {brand}-decon.md  ← 单品牌 5 核维深拆
    └── intelligence.md   ← 情报监控方案（行业信源 / 本地竞争监控）
```

**重跑/刷新语义**：同目标重跑 = 覆盖更新（Quick→Standard→Deep 递进升级）；需保留旧版时目录追加序号（`{slug}-2/`）；`--refresh` 在原目录内增量更新。**安全检查（源自真实事故）**：落盘前先确认目标目录——若已存在且文件修改时间早于本次会话开始（即非本会话产出），一律换新序号目录，禁止覆盖；已发生覆盖时在该目录写 INCIDENT.md 记录。

Quick 模式只产出 `report.html`，不写 `analysis.md`。

### report.html 规格（按深度缩放）

| 要素 | Quick | Standard | Deep |
|------|-------|---------|------|
| Hero 封面+结论徽章 | ✅ | ✅ | ✅ |
| KPI 卡片 | 3 张 | 4 张 | 5 张 |
| Sidebar 导航 | ❌ | ✅ | ✅ |
| 图表 | ❌ | ✅ 机会金矩阵（内置 CSS 网格，离线可用） | ✅ 矩阵 + 需求趋势图（Chart.js，带离线回退） |
| 竞品对照 | 简表（5 行，仅 S1 Top 5） | 完整表 | 完整表+深拆摘要 |
| 机会卡片（含反方理由标签） | ✅ 3 张 | ✅ 4-5 张 | ✅ 5-8 张 |
| 行动指南 | ❌ | ✅ | ✅ |
| 情报摘要 | ❌ | ❌ | ✅（默认摘要；`--full-report` 展开） |
| 下一步建议（带指令）+ 使用引导 | ✅ | ✅ | ✅ |
| 复盘 Prompt（按深度裁剪） | ✅ | ✅ | ✅ |

**`--full-report`**：Standard/Deep 下加此开关，report.html 包含 analysis.md 完整内容与完整情报系统；默认精简，细节在 analysis.md。

**技术规范**：
- **必须基于 `references/report-template.html` 生成**：设计 tokens 与版式骨架不改动，按 [所有档位]/[S+]/[Deep] 注释裁剪区块
- 主观评估（能力对照、打分）一律以表格呈现并标注「拆解评估」；唯一图表化的是机会金矩阵（CSS 网格）与 Deep 档趋势图（引用真实数据，带来源）
- 单文件自包含（除 Chart.js CDN）、系统字体栈、内联样式、浅色主题、响应式、`max-width: 1400px`、`@media print` 隐藏 sidebar、页内锚点不链接 .md 文件
- **deep-dive 引用一律用页内弹层**：生成时把 deep-dive/*.md 完整内容内嵌为 `<script type="text/markdown" id="md-xxx">`，引用处用 openMd() 弹出渲染（模板内置轻量 markdown 渲染器：标题/表格/列表/加粗/引用/代码，离线可用，Esc 关闭）——禁止"完整版见 xxx.md"式死引用

---

## 六、流程详解

### S1：行业素描

**范围锁定**（硬规则 1）先行，然后回答 6 个必答问题：

```
1. 一句话定义：这个行业在做什么？谁付钱？为什么付钱？
2. 市场规模：量级 + 增长趋势（↑/→/↓）+ 一句话理由（硬规则 3/5）
3. 核心玩家 Top 5：排名 + 一句话定位
4. 产业链位置：上下游是谁？钱在哪一环流动？
5. 当前阶段：起步期/成长期/成熟期/衰退期/转型期
6. 需求侧画像：典型客户？付费动机与决策链？需求在涨、平还是在迁移？
```

**数据来源**：WebSearch 3-4 次（含 1 次需求侧检索）。**冷门行业降级**按硬规则 7。

### S1.5：初步判断（押注）★ v2.0 核心新增

S1 结束立即输出，不等证据收集完：

```
🎯 初步判断：{值得入场 / 谨慎观察 / 不建议}——{一句话理由}
该判断成立所依赖的关键假设（2-3 条，作为后续检索的靶子）：
  A1: {如"需求增长主要来自存量翻新"}
  A2: {如"本地无品牌化对手"}
```

S2/S2.5 的检索必须优先瞄准 A1-A3：**找到反证要记录，找不到反证也是结论的一部分**。

### S2：格局拆解

**玩家分类表**（3-5 代表：巨头/挑战者/新锐各 ≥1）。年营收：上市公司用财报；非上市公司给估算方法（门店数×客单×周转等）或「未公开」。

**竞品 5 核维**（Standard 3 个，Deep 5-8 个；单品牌按适配规则）：

| # | 维度 | 问什么 |
|---|------|--------|
| 1 | 定位与赚钱模式 | 卖什么？利润从哪来？服务是否货币化？ |
| 2 | 内容与获客引擎 | 平台策略？投放？把人拉进来的真实机制？ |
| 3 | 转化链路 | 从看到内容到成交几步？哪里在漏人？有无度量？ |
| 4 | 差异化武器 | 别人抄不走的什么？（技术/渠道/认知/成本/关系） |
| 5 | 可复制性 | ⭐=随便抄 ⭐⭐=需资源 ⭐⭐⭐=几乎抄不走（附威胁临近度） |

工具栈推断参考 `references/china-tools.md`（入表前验证在营）。

**数据来源**：每品牌 WebSearch 2-3 次，检索词优先覆盖 S1.5 假设的反面（如押"本地无对手"，就搜"区域 集合店/新店开业"）。

### S2.5：外围生态扫描

WebSearch 2-3 次（"行业名+工具/代运营/新兴概念/趋势"+当前年份限定词）。产出四类发现：新兴服务商 / 新兴工具平台 / 新兴营销概念 / 关键词趋势——**直接融入 S3 机会判断，不孤立记录**。

### S3：机会判断

**3.1 机会清单（3-5 条）**：

```
🟢/🟡/⚪/🔴 机会名称
- 为什么是机会：（2-3 句逻辑链，必须引用需求侧证据）
- 最强反方理由：（一句话——这条机会最可能死在哪）
- 确定性：高/中/低
- 窗口期：0-12 个月 / 12-24 个月 / 已关闭
- 如果有人已经在做：（竞品名）
```

**3.2 机会金矩阵**：窗口期映射：0-12=开放；12-24=开放（标"收窄"）；已关闭=关闭列。空象限标注"空"+原因（硬规则 7）。

**3.3 风险预警（3 条）**：最大的坑、最容易死的环节——禁止"竞争激烈"式空话。

**3.4 6-12 个月预测**：方向、升降、变数。

**数据来源**：在 S1-2.5 基础上补 1-2 次定向搜索（"行业名+政策/监管"、"行业名+融资/关店/裁员"+当前年份）。**押注对账**：S1.5 的初步判断在此修正或维持——被推翻要写明被什么证据推翻；未被推翻也要列出"找过但没找到反证"的检索词。

### S4：行动指南

```
1. 最佳切口：哪个细分切入？为什么？
2. 具体打法：第一步做什么？前三步怎么做？
3. 单位经济速算：客单 × 毛利 × 月成交 vs 获客成本 → 回本周期；无基准给区间标「估算」
4. 资源需求：钱/人/时间
5. 避坑清单：最容易踩的 3 个坑 + 怎么躲开
```

数据来源：以 S1-3 综合为主；资源/成本可补 1 次"行业名+开店/加盟成本"搜索。

---

## 七、Quality Gates（9 道关键闸门）

| # | 闸门 | 通过标准 |
|---|------|---------|
| G1 | 范围与押注 | 范围已锁定并注明；S1.5 押注+关键假设已输出 |
| G2 | 证据可审计 | 关键数字带年份+口径+来源；`sources.md` 已落盘（转引已标级）；未公开规则已执行 |
| G3 | 反证完成 | 每条机会有最强反方理由且引用需求证据；S3 含押注对账（修正/维持+依据） |
| G4 | 矩阵诚实 | 四象限要么映射机会、要么"空"+原因；窗口期映射正确 |
| G5 | 单位经济 | 已速算或明确标注数据不足 |
| G6 | 报告合规 | 基于官方模板（tokens 未改、区块按深度裁剪）、hero 有结论徽章、无伪精确图表、deep-dive 引用为页内弹层（md 已内嵌，无死链） |
| G7 | 收尾件套 | 3 条建议带可复制指令 + 使用引导 + 复盘 Prompt（Standard/Deep 双落点；Quick 仅 html；Quick 用假设审查版模板） |
| G8 | Deep 铁律 | `deep-dive/intelligence.md` 已生成且达硬规则 10 标准；report 内嵌摘要 |
| G9 | 刷新纪律 | （仅 Refresh）≤5 次检索、三分支处理正确、sources.md 追加、变更可追溯 |

---

## 八、收尾与复盘

### 8.1 下一步建议（必有）

每份产出末尾给出 **3 条建议**，每条 = 具体建议 + 一句话理由 + 可复制指令。必须基于本次分析的具体发现。

**使用引导（必有）**：建议之后用 2-3 行告诉用户"接下来想怎么问"：想深挖品牌 → 「深度拆解 {品牌} --deep」；想持续跟踪 → 「按情报方案出第一份周报」；有异议 → 复制复盘 Prompt；想换范围 → 「用 {新范围} 重跑 {目标}」。

### 8.2 复盘 Prompt（必有）

```
🪞 复盘 Prompt（复制以下内容发给 AI 追问）

我正在研究 [{target}]，已完成 {depth} 深度拆解。
核心发现：{2-3 条关键结论}
识别机会：{N} 个

请帮我审查：
1. 机会清单的推理链是否成立？押注对账是否诚实？
2. 有没有遗漏的重要品牌/服务商/新兴概念？
3. 行动指南的可执行性如何？
```

**按深度裁剪**：Quick 未跑 S2/2.5，第 2 条替换为"核心结论依赖哪些假设？假设不成立时结论如何变化？"——避免诱导编造。

---

## 九、中国市场适配

默认面向**中国市场**；遇海外行业自动切换对应平台与工具。

- 平台默认：抖音/小红书/微信视频号/公众号/知乎/B站；投放默认：巨量引擎/巨量本地推/小红书聚光；私域默认：企微/微盟/有赞
- 不套用 Shopify/Klaviyo/Yotpo 等海外概念
- 检索用中文关键词（通用搜索引擎，无法指定百度/微信搜一搜索引；微信生态用"品牌名+公众号/小程序"间接检索）；SEO 软文混杂，按硬规则 3 交叉验证

完整工具栈：`references/china-tools.md`（长尾工具先验证在营再引用）。

---

## 十、评测用例（golden cases）

回归测试见 `references/golden-cases.md`（Quick / Standard / Deep 单品牌 / 意图映射 / Refresh 五个 Case + 结构断言）。每次修改本技能或模板后逐条回归。

---

## 版本

当前 **v2.0.2**。完整版本史与每条规则的来源追溯见 [CHANGELOG.md](CHANGELOG.md)——改动本技能时必须同步追加。
